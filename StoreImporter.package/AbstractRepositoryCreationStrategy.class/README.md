The repository creation strategy determines how SETT creates the git repository from the Store extracts.

Instance Variables:
	destinationConfiguration	<SettDestinationConfiguration>	specifies details of where things are written
	logger							<SettLogger>							writes messages to the log file
	storeCommitList				<SettCommitList>					the list of pundle versions to extract
