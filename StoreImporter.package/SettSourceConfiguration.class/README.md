Configuration specifying the location and credentials for a Store database, and what subset to extract.

The subset to extract is determined by providing a collection of pattern matching strings which will be used to filter the available pundles. The default patterns match everything. e.g. #('*')